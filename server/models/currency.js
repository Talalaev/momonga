/**
 * @swagger
 * definitions:
 *   Currency:
 *     properties:
 *       curency:
 *         type: string
 *       countryName:
 *         type: string
 *       iso4217Letters:
 *         type: string
 *       iso4217Numbers:
 *         type: string
 */

/**
 * Реальной модели в базе данных у сущьности Currency нет, данные берутся из файла.
 */
